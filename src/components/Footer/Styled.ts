import styled from "styled-components";
import { Layout } from "antd";

const { Footer } = Layout;

export const StyledFooter = styled(Footer)`
  padding: 10px 20px;
  color: #00152990;
  font-weight: 400;
  font-size: 12px;
`;
