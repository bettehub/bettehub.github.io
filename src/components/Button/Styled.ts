import styled from 'styled-components'
import { Button } from 'antd'

export const StyledButton = styled(Button)``
export const StyledResisterButton = styled(Button)`
  position: absolute;
  top: 55px;
  right: 20px;
`
